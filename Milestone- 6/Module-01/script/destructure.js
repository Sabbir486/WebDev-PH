// Object Call

const hena = {
    name: 'Hena',
    Age: 20,
    status: "Not Found"
}

// Destructuring on Object

const {name, Age, status} = hena;

console.log(name);
console.log(Age);



// Destructuring on Array

const person = ['haaa', 'raaa', 'taaa', {name:'sab'}];

const [actor, coder, developer, obj] = person;

console.log(coder);
console.log(actor);
console.log(developer);
console.log(obj);






